import {
  Button as Button$1,
  ButtonProps as ButtonProps$1,
} from '@mantine/core';

export interface ButtonProps extends ButtonProps$1 {}

export function Button(props: ButtonProps) {
  return <Button$1 {...props} />;
}
